name: patrol_sandbox
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  patrol: ^2.2.1

patrol:
  app_name: Patrol Sandbox
  android:
    package_name: com.example.patrolSandbox
  ios:
    bundle_id: com.example.patrolSandbox
flutter:
  uses-material-design: true
